# find() method returns the lowest index or first occurrence of the substring if it is 
# found in a given string. If it is not found, then it returns -1.

# Syntax: str_obj.find(sub, start, end)

word = 'geeks for geeks'
print(word.find('for'))